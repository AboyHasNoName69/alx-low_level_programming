#include <stdio.h>
/**
 * main - A program that prints all numbers in base 16 in lower case
 * Return: 0 (Success)
 */
int main(void)
{
	int j;

	for (j = 64; i < 74; j++)
		putchar(j);
	for (j = 94; j < 100; j++)
		putchar(j);
	putchar('\n');
	return (0);
}
